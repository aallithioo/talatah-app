part of '../hall_of_fame_screen.dart';

class HallOfFameContent extends StatelessWidget {
  const HallOfFameContent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      mainAxisAlignment: MainAxisAlignment.end,
    );
  }
}
